---
import BaseLayout from '../layouts/BaseLayout.astro';
import Section from '../components/Section.astro';
import Button from '../components/Button.astro';
---

<BaseLayout 
  title="Contact - Ghost-Note"
  description="Contactez Ghost-Note pour toute question, inscription ou demande d'information sur les formats proposés."
>
  <!-- Hero -->
  <Section class="text-center">
    <h1 class="text-4xl md:text-5xl font-bold text-fg mb-6">Contact</h1>
    <p class="text-xl text-muted max-w-2xl mx-auto">
      Une question ? Envie de vous inscrire ? N'hésitez pas à nous écrire
    </p>
  </Section>

  <!-- Formulaire -->
  <Section>
    <div class="max-w-2xl mx-auto">
      <form 
        action="mailto:contact@ghost-note.example"
        method="post"
        enctype="text/plain"
        class="space-y-6"
      >
        <!-- 
          Pour utiliser Netlify Forms, décommenter et remplacer mailto:
          <form name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field">
        -->
        
        <div>
          <label for="name" class="block text-sm font-medium text-fg mb-2">
            Nom *
          </label>
          <input 
            type="text" 
            id="name" 
            name="name" 
            required
            class="w-full px-4 py-3 rounded-lg border border-fg/20 bg-bg text-fg focus:outline-none focus:border-accent focus:ring-2 focus:ring-accent/20"
          />
        </div>

        <div>
          <label for="email" class="block text-sm font-medium text-fg mb-2">
            Email *
          </label>
          <input 
            type="email" 
            id="email" 
            name="email" 
            required
            class="w-full px-4 py-3 rounded-lg border border-fg/20 bg-bg text-fg focus:outline-none focus:border-accent focus:ring-2 focus:ring-accent/20"
          />
        </div>

        <div>
          <label for="format" class="block text-sm font-medium text-fg mb-2">
            Format qui vous intéresse
          </label>
          <select 
            id="format" 
            name="format"
            class="w-full px-4 py-3 rounded-lg border border-fg/20 bg-bg text-fg focus:outline-none focus:border-accent focus:ring-2 focus:ring-accent/20"
          >
            <option value="">Sélectionnez un format</option>
            <option value="cdmc">CDMC</option>
            <option value="ateliers">Ateliers</option>
            <option value="cours-individuels">Cours individuels</option>
            <option value="tutorat-ableton">Tutorat Ableton</option>
            <option value="autre">Autre</option>
          </select>
        </div>

        <div>
          <label for="message" class="block text-sm font-medium text-fg mb-2">
            Message *
          </label>
          <textarea 
            id="message" 
            name="message" 
            rows="6"
            required
            class="w-full px-4 py-3 rounded-lg border border-fg/20 bg-bg text-fg focus:outline-none focus:border-accent focus:ring-2 focus:ring-accent/20 resize-none"
          ></textarea>
        </div>

        <div>
          <button 
            type="submit"
            class="w-full px-6 py-3 bg-accent text-white rounded-lg hover:bg-accent/90 transition-colors font-medium"
          >
            Envoyer
          </button>
        </div>
      </form>

      <!-- 
        Alternative avec Formspree ou autre service :
        <form action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
          ... mêmes champs ...
        </form>
      -->
    </div>
  </Section>

  <!-- Réseaux -->
  <Section class="bg-fg/5">
    <div class="max-w-2xl mx-auto text-center">
      <h2 class="text-2xl font-bold text-fg mb-6">Nous suivre</h2>
      <div class="flex flex-wrap gap-4 justify-center">
        <a 
          href="https://facebook.com/ghost-note" 
          target="_blank" 
          rel="noopener noreferrer"
          class="px-6 py-3 rounded-lg border border-fg/20 hover:border-accent hover:text-accent transition-colors"
        >
          Facebook
        </a>
        <a 
          href="https://instagram.com/ghost-note" 
          target="_blank" 
          rel="noopener noreferrer"
          class="px-6 py-3 rounded-lg border border-fg/20 hover:border-accent hover:text-accent transition-colors"
        >
          Instagram
        </a>
        <a 
          href="mailto:contact@ghost-note.example"
          class="px-6 py-3 rounded-lg border border-fg/20 hover:border-accent hover:text-accent transition-colors"
        >
          Email
        </a>
      </div>
    </div>
  </Section>

  <!-- Mentions légales -->
  <Section>
    <div class="max-w-2xl mx-auto">
      <h2 class="text-2xl font-bold text-fg mb-4">Mentions légales</h2>
      <div class="prose prose-sm text-muted space-y-4">
        <p>
          <strong>Éditeur du site :</strong> Ghost-Note<br />
          <strong>Contact :</strong> contact@ghost-note.example<br />
          <strong>Hébergement :</strong> [À compléter]
        </p>
        <p>
          Les informations recueillies via le formulaire de contact sont utilisées uniquement pour répondre 
          à vos demandes et ne sont pas transmises à des tiers.
        </p>
      </div>
    </div>
  </Section>
</BaseLayout>

